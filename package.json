{
  "name": "aalto-fitech-code",
  "displayName": "Aalto-Fitech_code",
  "description": "WIP",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.125.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:wsdPlatform.checkPlatformStatus",
    "onView:wsdPlatform.account",
    "onView:wsdPlatform.courses"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "wsdPlatform",
          "title": "WSD Platform",
          "icon": "media/wsd-platform.svg"
        }
      ]
    },
    "views": {
      "wsdPlatform": [
        {
          "id": "wsdPlatform.account",
          "name": "Account"
        },
        {
          "id": "wsdPlatform.courses",
          "name": "Courses"
        }
      ]
    },
    "viewsWelcome": [
      {
        "view": "wsdPlatform.account",
        "contents": "Sign in to access your WSD Platform account.\n\n[Sign In](command:wsdPlatform.signIn)"
      }
    ],
    "commands": [
      {
        "command": "wsdPlatform.checkPlatformStatus",
        "title": "WSD Platform: Check Platform Status"
      },
      {
        "command": "wsdPlatform.signIn",
        "title": "WSD Platform: Sign In"
      },
      {
        "command": "wsdPlatform.showCurrentUser",
        "title": "WSD Platform: Show Current User"
      },
      {
        "command": "wsdPlatform.signOut",
        "title": "WSD Platform: Sign Out"
      },
      {
        "command": "wsdPlatform.refreshCourses",
        "title": "WSD Platform: Refresh Courses",
        "icon": "$(refresh)"
      }
    ],
    "menus": {
      "view/title": [
        {
          "command": "wsdPlatform.refreshCourses",
          "when": "view == wsdPlatform.courses",
          "group": "navigation"
        }
      ]
    },
    "configuration": {
      "title": "WSD Platform",
      "properties": {
        "wsdPlatform.apiBaseUrl": {
          "type": "string",
          "default": "http://localhost:8842/api",
          "description": "Base URL of the Fitech platform API."
        },
        "wsdPlatform.useMockApi": {
          "type": "boolean",
          "default": true,
          "description": "Use mock platform data instead of the real API."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run package",
    "compile": "npm run check-types && npm run lint && node esbuild.js",
    "watch": "npm-run-all -p watch:*",
    "watch:esbuild": "node esbuild.js --watch",
    "watch:tsc": "tsc --noEmit --watch --project tsconfig.json",
    "package": "npm run check-types && npm run lint && node esbuild.js --production",
    "compile-tests": "tsc -p . --outDir out",
    "watch-tests": "tsc -p . -w --outDir out",
    "pretest": "npm run compile-tests && npm run compile && npm run lint",
    "check-types": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/vscode": "^1.125.0",
    "@types/mocha": "^10.0.10",
    "@types/node": "24.x",
    "typescript-eslint": "^8.61.1",
    "eslint": "^10.5.0",
    "esbuild": "^0.28.1",
    "npm-run-all": "^4.1.5",
    "typescript": "^6.0.3",
    "@vscode/test-cli": "^0.0.15",
    "@vscode/test-electron": "^3.0.0"
  },
  "overrides": {
    "diff": "^8.0.4",
    "serialize-javascript": "^7.0.6"
  }
}
